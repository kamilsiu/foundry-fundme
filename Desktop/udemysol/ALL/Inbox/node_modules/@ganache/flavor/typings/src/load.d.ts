import { AnyFlavor } from "./flavor";
/**
 * Load the given package using node's `require` function. This is used to load
 * flavors plugins.
 * @param flavor
 * @returns
 */
export declare function load<F extends AnyFlavor>(flavor: F["flavor"]): F;
//# sourceMappingURL=load.d.ts.map