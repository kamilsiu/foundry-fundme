/// <reference types="node" />
import { TemplatedApp } from "@trufflesuite/uws-js-unofficial";
import { InternalServerOptions } from "../types";
import type { WebsocketConnector, WebSocket } from "@ganache/flavor";
export declare function sendFragmented(ws: WebSocket<void>, data: Generator<Buffer, void, void>, useBinary: boolean, chunkSize: number): void;
export type GanacheWebSocket = WebSocket<void> & {
    closed?: boolean;
};
export type WebsocketServerOptions = Pick<InternalServerOptions["server"], "wsBinary" | "rpcEndpoint" | "chunkSize">;
export declare const MAX_PAYLOAD_SIZE: number;
export default class WebsocketServer {
    #private;
    constructor(app: TemplatedApp, connector: WebsocketConnector<any, any, any>, options: WebsocketServerOptions);
    close(): void;
}
//# sourceMappingURL=ws-server.d.ts.map